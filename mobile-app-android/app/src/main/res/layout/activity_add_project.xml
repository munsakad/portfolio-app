<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent"
    android:background="@color/surface_dark" android:fillViewport="true">
    <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content"
        android:orientation="vertical" android:padding="24dp">

        <TextView android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="Add Project" android:textSize="24sp" android:textStyle="bold"
            android:textColor="@color/text_primary" android:layout_marginBottom="24dp"/>

        <EditText android:id="@+id/etTitle" android:layout_width="match_parent"
            android:layout_height="56dp" android:hint="Project Title *"
            android:background="@drawable/bg_card" android:padding="16dp"
            android:textColor="@color/text_primary" android:textColorHint="@color/text_muted"
            android:layout_marginBottom="12dp"/>
        <EditText android:id="@+id/etDescription" android:layout_width="match_parent"
            android:layout_height="100dp" android:hint="Description"
            android:background="@drawable/bg_card" android:padding="16dp"
            android:textColor="@color/text_primary" android:textColorHint="@color/text_muted"
            android:gravity="top" android:inputType="textMultiLine"
            android:layout_marginBottom="12dp"/>
        <EditText android:id="@+id/etTechStack" android:layout_width="match_parent"
            android:layout_height="56dp" android:hint="Tech Stack (e.g. React, Node.js)"
            android:background="@drawable/bg_card" android:padding="16dp"
            android:textColor="@color/text_primary" android:textColorHint="@color/text_muted"
            android:layout_marginBottom="12dp"/>
        <EditText android:id="@+id/etLiveUrl" android:layout_width="match_parent"
            android:layout_height="56dp" android:hint="Live Demo URL"
            android:background="@drawable/bg_card" android:padding="16dp"
            android:textColor="@color/text_primary" android:textColorHint="@color/text_muted"
            android:inputType="textUri" android:layout_marginBottom="12dp"/>
        <EditText android:id="@+id/etGithubUrl" android:layout_width="match_parent"
            android:layout_height="56dp" android:hint="GitHub URL"
            android:background="@drawable/bg_card" android:padding="16dp"
            android:textColor="@color/text_primary" android:textColorHint="@color/text_muted"
            android:inputType="textUri" android:layout_marginBottom="24dp"/>

        <Button android:id="@+id/btnSave" android:layout_width="match_parent"
            android:layout_height="56dp" android:text="Save Project"
            android:background="@drawable/bg_button" android:textColor="@color/brand_primary"
            android:textStyle="bold" android:layout_marginBottom="12dp"/>
        <Button android:id="@+id/btnCancel" android:layout_width="match_parent"
            android:layout_height="48dp" android:text="Cancel"
            android:background="@drawable/bg_card" android:textColor="@color/text_muted"/>
    </LinearLayout>
</ScrollView>
