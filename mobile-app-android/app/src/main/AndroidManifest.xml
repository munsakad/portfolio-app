<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.PortfolioApp">

        <activity android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.PortfolioApp.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.LoginActivity" android:exported="false" />
        <activity android:name=".activities.RegisterActivity" android:exported="false" />
        <activity android:name=".activities.MainActivity" android:exported="false" />
        <activity android:name=".activities.ProfileActivity" android:exported="false" />
        <activity android:name=".activities.ProjectsActivity" android:exported="false" />
        <activity android:name=".activities.ContactActivity" android:exported="false" />
        <activity android:name=".activities.AnalyticsActivity" android:exported="false" />
        <activity android:name=".activities.AddProjectActivity" android:exported="false" />
        <activity android:name=".activities.AddSkillActivity" android:exported="false" />

    </application>
</manifest>
